<UserControl x:Class="KisManager.Views.TabAreaSalesPerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KisManager.Views"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource DefaultBorder}" TargetType="Border">
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style x:Key="QueryPageTop" BasedOn="{StaticResource QueryPageBorder}" TargetType="Border">
            <Style.Resources>
                <Style TargetType="Label">
                    <Setter Property="Margin" Value="3 0"/>
                    <Setter Property="FontFamily" Value="微软雅黑"/>
                </Style>
                <Style TargetType="CheckBox" BasedOn="{StaticResource MetroCheckBox}">
                    <Setter Property="FontFamily" Value="微软雅黑"/>
                    <Setter Property="Margin" Value="3 0"/>
                </Style>
                <Style TargetType="ComboBox" BasedOn="{StaticResource MetroComboBox}">
                    <Setter Property="FontFamily" Value="微软雅黑"/>
                    <Setter Property="Margin" Value="3 0"/>
                </Style>
                <Style TargetType="Button" BasedOn="{StaticResource MetroButton}">
                    <Setter Property="FontFamily" Value="微软雅黑"/>
                    <Setter Property="Margin" Value="3 0"/>
                    <Setter Property="MinWidth" Value="60"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="QueryPageContent"  BasedOn="{StaticResource QueryPageBorder}" TargetType="Border">

        </Style>
    </UserControl.Resources>
    <DockPanel >
        <Border Style="{StaticResource QueryPageTop}" DockPanel.Dock="Top">
            <Grid>
                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                    <Label>筛选条件</Label>
                    <ComboBox></ComboBox>
                    <Button>查询</Button>
                    <Button>导出</Button>
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <CheckBox>条件1</CheckBox>
                    <CheckBox>条件2</CheckBox>
                    <CheckBox>条件3</CheckBox>
                </StackPanel>
            </Grid>
        </Border>
        <Border Style="{DynamicResource QueryPageContent}">
            <DataGrid >

            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
